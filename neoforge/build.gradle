apply from: "https://raw.githubusercontent.com/ToCraft/GradleMaster/v2/neoforge.gradle"

dependencies {
    modApi "dev.tocraft:craftedcore-neoforge:${parent.name}-${rootProject.craftedcore_version}"
}