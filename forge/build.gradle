loom {
    forge {
        mixinConfigs = ["walkers-forge.mixins.json", "walkers.mixins.json"]
    }
}

apply from: "https://raw.githubusercontent.com/ToCraft/GradleMaster/v2/forge.gradle"

dependencies {
    modApi "dev.tocraft:craftedcore-forge:${parent.name}-${rootProject.craftedcore_version}"
}